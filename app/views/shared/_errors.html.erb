<% if obj.errors.any? %>
	<div class = "alert alert-error">
		<p>Please fix the following errors:</p>
		<ul>
		<% obj.errors.full_messages.each do |err| %>
			<li><%= err %></li>
		<% end %>
		</ul>
	</div>
<% end %>